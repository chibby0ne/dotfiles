# simple configuration file for developing
# one big pane for editing code
# another small pane at the bottom for testing and setting things up in console

# select the first (0) pane
select-pane -t 0 
# split it into one pane below
split-window -v -p 25 
# split top pane into two horizontally
select-pane -t 0 
split-window -h -p 50
# go back to the first pane
select-pane -t 0 
